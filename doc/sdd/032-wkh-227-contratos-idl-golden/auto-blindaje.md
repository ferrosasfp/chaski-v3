# Auto-Blindaje — WKH-227 / HU-SOL-24

### [2026-07-22 23:57] Wave W2 (facilitator) — lint `--max-warnings 0` rojo por security plugin
- **Error**: primer `npm run lint` falló con 3 warnings: `security/detect-object-injection` en `canonical-hash.ts` (lookup `obj[k]`) y 2× `security/detect-non-literal-fs-filename` en `escrow-idl.hash.test.ts` (`existsSync`/`readFileSync` con path var).
- **Causa raíz**: el repo corre eslint con `--max-warnings 0`; el security plugin marca todo lookup dinámico / fs con arg no literal, aunque el input sea seguro (keys de `Object.keys`, path build-time constante).
- **Fix**: seguí la convención EXISTENTE del repo — inline `// eslint-disable-next-line security/detect-object-injection -- <justificación>` (idéntico a `src/core/idempotency.ts:120`) y file-level `/* eslint-disable security/detect-non-literal-fs-filename -- ... */` en el test (idéntico a `src/__tests__/unit/no-console.test.ts:1`). No se cambió la lógica.
- **Aplicar en**: cualquier helper nuevo que itere `Object.keys()` o cualquier test que lea archivos por path variable en este repo → necesita el disable justificado o el lint bloquea. Verificar `npm run lint` ANTES de dar por cerrada la wave, no solo `tsc`.

### [2026-07-23 00:00] Wave W1 (remit-agents) — grounding FALSO del Story: `vitest.config.ts` SÍ tiene `include`
- **Error**: el Story §2 afirma «`vitest.config.ts` NO define `include` → `contracts/**/*.test.ts` SÍ se ejecuta con `npm test` ✓». Es FALSO: `vitest.config.ts:12` tiene `include: ["src/**/*.test.ts"]`. `npm test` corre 160 tests en 9 files (todos bajo `src/`) y NO recoge `contracts/contracts.provider.test.ts`.
- **Causa raíz**: el Architect asumió el glob default de vitest sin leer el config real del repo. El repo restringe `include` a `src/`.
- **Fix (W1, sin salir de scope)**: implementé los 5 archivos en `contracts/` tal cual el Scope IN + verifiqué el contract test verde corriéndolo con un config de override TEMPORAL en la raíz del repo (`--include contracts/**`), borrado inmediatamente (NO deliverable). NO modifiqué `vitest.config.ts` (fuera de Scope IN + CD-1 «cero cambio runtime»). El contract test pasaba 6/6 aislado. Escalado al orquestador.
- **Resolución (orquestador, mismo criterio que W2/facilitator)**: reubicar TODO `contracts/` → `src/contracts/` (fixtures + test juntos) para que el `include: ["src/**/*.test.ts"]` lo levante, SIN tocar `vitest.config.ts`. Ajustados los imports relativos a los agentes (`../src/agents/*` → `../agents/*`). Verificado: `npm test` sube de 160 (9 files) → **166 (10 files)**, contract test 6/6 dentro de la suite; `tsc --noEmit` limpio; `vitest.config.ts` sin diff.
- **Aplicar en**: cualquier wave que agregue tests fuera de la carpeta que el `vitest.config.ts`/`jest.config` del repo incluye → verificar el `include`/`testMatch` REAL antes de asumir que el runner los levanta. El Dev debe correr `npm test` y confirmar que el conteo de test-files SUBIÓ, no solo que «pasó».

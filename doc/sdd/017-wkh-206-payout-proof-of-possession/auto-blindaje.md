# Auto-Blindaje — WKH-206 (F3 Dev)

### [2026-07-16 11:33] Fix-pack AR-MNR-1 — `pop.prove()` FUERA del try/catch + `HttpPopSigner` divergía de DT-2 (501 lanzaba en vez de skip)
- **Error**: (1) `pop.prove(address)` se invocaba en `confirm-and-send.ts:221-225`, ANTES del `try` que abre en :226. Un throw de `prove()` en modo real (settlement inyectado, `markPrincipalIn` ya ocurrió en :196) escapaba `execute()` sin capturar → la remesa quedaba VARADA en `principal_in` sin auto-refund + error crudo a la UI. (2) `http-pop-signer.ts:18` lanzaba en CUALQUIER `!res.ok`, incluido el 501 (`pop_not_configured`, server-OFF), divergiendo del contrato de `sdd.md` DT-2 §82 ("el HttpPopSigner trata 501 como skip, no adjunta campos").
- **Causa raíz**: patrón recurrente "código con I/O fuera del try/catch del use-case" (mismos precedentes WKH-168/C3 `settle` y WKH-202 `req.json`). Y el adapter colapsaba dos resultados no-felices distintos (mecanismo-apagado vs error-genuino) en un único throw.
- **Fix**: (a) `PopSigner.prove` (ports.ts) ahora devuelve `{...} | null`: `null` = SKIP (501), throw = fail-closed controlado (otros errores). (b) `HttpPopSigner`: `if (res.status === 501) return null;` ANTES del `if (!res.ok) throw`. (c) el bloque `if (this.pop)` se MOVIÓ dentro del `try` existente del submit → un throw de `prove()` cae en el `catch` que ya hace `failAndRefund(r, ..., principalReallyIn)` ⇒ degrada controlado (AC-6 `principal_settled_refund_manual`), NUNCA escapa ni deja la remesa varada. (d) `FakePopSigner` + tipo actualizados (CD-15). Es una CORRECCIÓN hacia DT-2, no una divergencia: comentario citando DT-2 dejado en ambos archivos.
- **Verificación**: 2 tests nuevos en `confirm-and-send.test.ts` (501→SKIP submit sin campos + prove-throw en real ⇒ degrada controlado, execute() resuelve) + 1 en `http-pop-signer.test.ts` (501→null, !ok-no-501→throw). Mutante (catch re-lanza) → mata el test de degradación controlada. `npm run qa` verde: 397 tests (394 baseline + 3).
- **Aplicar en**: TODO adapter/use-case con I/O justo antes del bloque de submit: la llamada I/O va DENTRO del try que ya degrada el submit. Y todo adapter HTTP que distinga "mecanismo apagado" (skip, valor centinela) de "error genuino" (throw) — no colapsarlos en un throw único.

### [2026-07-16 11:10] Wave 2 — `mock.calls[0]` tipado como `[] | undefined` (tsc TS2352/TS2488)
- **Error**: en `http-pop-signer.test.ts`, `const [url, init] = fetchMock.mock.calls[0] as [string, RequestInit]` falló typecheck: `vi.fn(async () => ...)` sin parámetros infiere `calls: []`, y `calls[0]` es `[] | undefined` → el `as` y el destructuring no compilan.
- **Causa raíz**: el mock de `fetch` se declaró sin firma de parámetros, así que TS no supo que las llamadas llevan `(url, init)`. Además `calls[0]` puede ser `undefined`.
- **Fix**: dar params explícitos al mock — `vi.fn(async (_url: string, _init?: RequestInit) => ...)` — y destructurar con fallback: `const [url, init] = fetchMock.mock.calls[0] ?? [];` + `init?.body`.
- **Aplicar en**: cualquier test que lea `mock.calls[i]` de un `vi.fn` cuyo cuerpo no declara los parámetros. Tipar el mock o castear vía `unknown`. El gate que lo cazó fue `npm run qa` (tsc), NUNCA `npm run build`.

### [2026-07-16 11:02] Wave 1 — off-by-one al verificar la byte-identidad del bloque WKH-168 por `md5sum`
- **Error**: al re-md5ear el bloque `:113-201` tras insertar el guard 7, extraje el rango `START..END-2` (usando el comentario `A10` como ancla), lo que dio un md5 distinto y una falsa alarma de "bloque modificado".
- **Causa raíz**: el bloque WKH-168 termina en la línea INMEDIATAMENTE anterior al comentario `// A10 — todo OK → forward` (no dos líneas antes). Resté 2 en vez de 1.
- **Fix**: extraer `START..(END-1)` donde `END` = línea del comentario `A10`. md5 = `1bf51c6e72dff0a150c4cc7408acc59c`, idéntico a `git show HEAD:...` → CD-5 confirmado.
- **Aplicar en**: toda verificación de byte-identidad por rango de líneas (WKH-168/202/206). Confirmar los límites del rango con `sed -n` + inspección visual de las líneas frontera ANTES de confiar en el md5; el ancla textual (`A10`) NO es parte del bloque protegido.
